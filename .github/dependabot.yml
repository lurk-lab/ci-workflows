version: 2
updates:
  - package-ecosystem: "github-actions"
    # "/" is a special case meaning `.github/workflows/`; composite action
    # directories must be listed separately for their dependencies to be seen
    directories:
      - "/"
      - "/.github/actions/**"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7